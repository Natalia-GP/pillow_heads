const Input = () => {
  return (
    <input
      className="contact__form__name__input"
      type="text"
      id="fullName"
      name="fullName"
      placeholder="Nombre..."
      required
    ></input>
  );
};

export default Input;
